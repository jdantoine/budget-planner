using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace JDBudgetPlanner.Definitions
{
    public class Definition
    {
        //HttpContext (and HttpContextBase) object holds information about the current HTTP request. It contains the Request and Response objects, as well as all the other context objects related to the request like User, Session, Profile, Cache, etc. It's created per-request.

        //DbContext contains the methods and properties for database communication.

        //Enums are strongly typed constants. They allow you to assign symbolic names to int values.
    }
}